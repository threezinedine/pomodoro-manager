# Execute Command

**Important**: Read `templates/command-common.md` for general guidelines and instructions that all commands must follow.

**Important**: Before executing, reread the `design/test-command.md` design document to ensure that all necessary tests have been planned and are ready for execution.

With the user input, execute the tests as planned in the `design/test-command.md` document. Focus on running the tests and collecting results without modifying any design documents or task lists.

Note that you MUST run all tests to verify the implementation, and you should report the results of each test back to the user. If any test fails, provide detailed information about the failure to help the user understand what went wrong and how to fix it.

**Important**: Only implement the tasks that i request, do not execute any additional tasks that are not explicitly requested by the user. This ensures that we stay focused on the specific tests that need to be executed without deviating from the planned scope.

**Note:** Update the `design/tasks.md` file to reflect the current state of tasks after executing the tests. This ensures that the task list remains an accurate source of truth for project progress and helps in tracking which tests have been executed and their outcomes.

**Note:** If you wanna modify the files, do not use `cat > ...`, modify it by yourself.