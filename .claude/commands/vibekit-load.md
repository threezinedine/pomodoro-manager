# VibeKit Load Command

Now load all the contents from the .claude file `templates/*` into the system, also loading the `design/*` files as well. This will ensure that all the necessary templates and design documents are available for reference and use in future commands and tasks.